# To do list for audio processing

## Data import
- import.py to index files in sqlite db
- add metadata in db

## Data cleaning
- clean.py to split raw audio in separate clips
- import audio sequences in db (with fk to original audio)

## Data labeling
- labels.py to classify audio sequences based on previous defined categories

## Machine learning
FFT on audio sequences to use as input on a not defined model
